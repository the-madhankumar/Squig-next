/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["assets.aceternity.com", "api.microlink.io",'avatars.githubusercontent.com'],
  },
};

export default nextConfig;
